[project]
name = "tomymind"
version = "0.1.0"
description = "Multi-source bookmark importer for mymind.com"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
dependencies = [
    "pydantic>=2.9",
]

[project.optional-dependencies]
scraper = [
    "playwright>=1.49",
    "typer>=0.13",
    "python-dotenv>=1.0",
]
stealth = ["tf-playwright-stealth>=1.1.0"]
importer = [
    "nats-py>=2.9",
    "httpx>=0.27",
    "pyjwt>=2.9",
]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "ruff>=0.7",
]

[project.scripts]
tomymind = "tomymind.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tomymind", "src/mymind_importer"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.ruff.lint.per-file-ignores]
# Typer requires typer.Option / typer.Argument calls in function defaults.
"src/tomymind/cli.py" = ["B008"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
